<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.qc.seclass.glm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GLM">
        <activity android:name=".searchItemName"
            android:label="Items List" ></activity>
        <activity
            android:name=".searchCategory"
            android:label="Grocery List" />
        <activity android:name=".gridLayout" />
        <activity
            android:name=".MyItems"
            android:label="Grocery List" />
        <activity
            android:name=".createLists"
            android:label="Create New Lists"
            android:parentActivityName=".UserLists" />
        <activity
            android:name=".UserLists"
            android:label="Grocery List" />
        <activity android:name=".renamelist" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>